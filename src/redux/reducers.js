import {combineReducers} from 'redux'

// const recipes = (state = [], action) => {
//   switch(action.type){
//   case 'ADD_RECIPE':
//     return [...state, action.value]
//     default: 
//     return state;
//   }
// }
export default combineReducers({})//if function above is used add "combineReducers({recipes})"

